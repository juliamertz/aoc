use super::*;

pub fn solve(input: Input) -> u32 {
    println!("{}",input);

    // Find all matching frequencies and their positions

    // Generate combinations for each found frequency
    
    // calculate x/y offset

    // place antinode at both antenna's + offset (if in bounds)

    10
}
